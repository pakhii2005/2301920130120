import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  server: {
    port: 3000, // Forces server to use your requested localhost:3000 mapping rule
    strictPort: false
  }
})
